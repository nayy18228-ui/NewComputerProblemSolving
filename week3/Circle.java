void main() {
       IO.print("Enter radius: ");
       int radius = Integer.parseInt(IO.readln());

       double area = 3.14 * radius * radius;

       IO.println("Area of the circle = " + area + " square units");
}